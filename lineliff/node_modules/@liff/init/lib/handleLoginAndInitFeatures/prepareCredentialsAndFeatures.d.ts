import { CREDENTIAL_OBJECTS } from '@liff/consts';
interface ReturnValue {
    credentials: CREDENTIAL_OBJECTS;
    features?: string[] | null;
}
export declare function prepareCredentialsAndFeatures(liffId: string): Promise<ReturnValue>;
export {};
